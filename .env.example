# Example environment variables for AI GitHub Actions workflows

# OpenAI-compatible API endpoint (e.g., https://api.openai.com)
OPENAI_API_ENDPOINT=

# OpenAI API key (store this as a GitHub secret, not in .env)
OPENAI_API_KEY=

# Model name for AI tasks (e.g., gpt-3.5-turbo, gpt-4)
# OPENAI_MODEL=gpt-3.5-turbo
